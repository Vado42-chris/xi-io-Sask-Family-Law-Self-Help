# sam_law Re-onboarding Delta — 2026-08-12

Status: `RECOVERY / KICKOFF REFRESH / PASS_WITH_FINDINGS / NO NEW IMPLEMENTATION AUTHORITY`  
Project: `sask_family_law_self_help`  
Product direction: `xi-io: sam_law` Saskatchewan jurisdiction package  
Repository: `Vado42-chris/xi-io-Sask-Family-Law-Self-Help`  
Current mutation lane: PR `#6` / `chore/framework-recovery-2026-08-12`  
Framework accepted-main observation for this refresh: `Vado42-chris/xi-io.net@151213e01e7c715d251273a3f0b7903821f36045`

```yaml
generated_not_framework_truth: true
operation: re_onboard_existing_project
project_recreation: false
accepted_product_truth_preserved: true
framework_compliance_claimed: false
runtime_verified: false
legal_currentness_verified: false
private_case_data_allowed: false
new_branch_authorized: false
merge_authorized: false
```

## 1. Purpose

This is the current kickoff/re-onboarding delta for sam_law. It compares the original 2026-07-22 new-repository bootstrap and the 2026-08-12 recovery record against the framework's newer managed-project direction without rewriting historical evidence or pretending that open framework planning work is already accepted implementation truth.

The goal is to make a fresh human or external worker able to answer, from repository-local public-safe evidence:

```text
WHAT PRODUCT IS THIS?
WHAT IS ACCEPTED STRATEGIC INTENT?
WHAT IS CURRENT AGILE/RECOVERY WORK?
WHAT EXACT MUTATION LANE IS ACTIVE?
WHAT LEGAL SOURCES ARE ADMITTED VS INTAKE-ONLY?
WHAT FRAMEWORK/PROCESS STATE IS ADOPTED VS BLOCKED?
WHAT PRIVACY/RIGHTS/SECURITY LIMITS APPLY?
WHAT MAY THE WORKER DO NOW?
WHAT MUST IT NOT DO?
WHAT IS THE NEXT SAFE ACTION?
```

This record is a recovery/control-plane artifact. It does not promote legal source material, create runtime behavior, migrate `xi/` to `xiio/`, enable AI/provider access, or authorize court/email transmission.

## 2. Historical kickoff verdict

### 2026-07-22 startup — preserve as historical evidence

The July startup correctly established:

- a distinct public managed product,
- human-only operation as the core path,
- AI as optional and subordinate,
- strong privacy separation from private case data,
- legal-information rather than legal-advice positioning,
- blocked court/service/provider transmission,
- donor boundaries for DocuForge/private evidence workspaces,
- synthetic-data-first implementation intent.

It also made an important but incomplete first-slice assumption:

```text
first reference slice = Kit #3J eligibility triage + task plan
```

That was reasonable for an empty-repository proof, but it did not first prove the full Saskatchewan self-help-kit/product boundary. The project therefore accumulated a single-kit/global-snapshot bias that is now being corrected.

### 2026-08-12 recovery — preserve and extend

The August recovery correctly changed the project interpretation to:

```text
MULTI-KIT JURISDICTION PACKAGE
+
FORMS + PROCEDURE/SELECTION RULES AS ONE GOVERNED SOURCE UNIT
+
INDEPENDENT SOURCE-FAMILY VERSIONING
+
FAIL-CLOSED SOURCE CURRENTNESS / CONFLICTS
+
PR #5 AS DONOR, NOT MERGE AUTHORITY
+
LEGACY xi/ PRESERVED WHILE xiio/ MIGRATION IS BLOCKED
```

This remains the correct recovery basis.

## 3. Re-onboarding classification

Use these states for this delta:

```text
PRESERVE
ADD
UPDATE
MIGRATE
NOT_APPLICABLE
BLOCKED
UNKNOWN
```

| Managed-project domain | State | Current evidence / required action |
|---|---|---|
| Repository/project identity | `PRESERVE` | Repo, project ID, public visibility and Saskatchewan-jurisdiction role are resolved. |
| Product scope | `UPDATE` | Explicitly multi-kit/all-applicable-family-law-self-help direction; not JCC-only. |
| Human-only path | `PRESERVE` | Required and already explicit. |
| AI optionality | `PRESERVE` | AI remains optional/subordinate; no AI runtime accepted. |
| Public/private data boundary | `PRESERVE` | Public repo must never contain real matter data. |
| Legal-information boundary | `PRESERVE` | No legal-advice or autonomous legal authority claim. |
| Legal source model | `UPDATE` | Replace single global snapshot assumption with independently versioned source families after Q1 gate. |
| Source-family completeness | `ADD` | Kickoff must require inventory of forms **and** procedural/selection/supporting-document/service/filing/follow-up rules for each admitted kit. |
| Cross-kit equivalence | `ADD` | Repeated form number/name is not equivalence proof; explicit equivalence/divergence review required. |
| Source currentness | `UPDATE` | Current-official comparison is a separate gate; structural capture does not prove currentness. |
| Accepted strategic/waterfall plan projection | `ADD / BLOCKED` | Product vision/architecture exist, but one typed accepted PlanRevision/current phase/gate projection is not yet present on accepted `main`. Do not invent final filename/schema before framework owners settle. |
| Current agile/work-cycle projection | `ADD` | Recovery queue exists, but it must be projected separately from strategic intent and active execution. |
| Active execution/custody projection | `ADD IN PR #6` | `docs/ops/ACTIVE_WORK_CHECKPOINT.md` and `docs/ops/CURRENT_LANE_STATUS.md` are added as thin projections over the existing execution plan. |
| Point-of-Order / continuation | `PRESERVE + UPDATE LATER` | Current execution plan preserves return semantics; future canonical Work Continuation integration remains framework-owned. |
| Task Context | `BLOCKED / FUTURE ADOPTION` | Public worker contract exports required preflight dimensions; canonical Task Context/admission integration is not yet adopted here. |
| Managed-project process/adoption lock | `BLOCKED` | Product must not silently inherit framework main. Exact versioned adopter-lock mechanism remains framework-owned. |
| Portable namespace | `MIGRATE / BLOCKED` | Legacy `xi/` is preserved; new `xiio/` writes wait for framework namespace/read-order prerequisite and reviewed migration. |
| Local `.xiio/` boundary | `PRESERVE AS RULE` | Local/private/runtime state must remain outside portable repo truth; no local runtime state is being added in this public repo. |
| Project lexicon/profile/index | `MIGRATE LATER` | Legacy files exist under `xi/`; refresh/migration waits for accepted portable contract shape. |
| Safe/blocked command classes | `PRESERVE` | README/AGENTS/execution plan already expose them. |
| Verification runbook | `ADD IN PR #6` | Thin `docs/ops/verification-runbook.md` added; structural checks remain limited claims. |
| Active-work do-not-touch rules | `ADD IN PR #6` | Thin `docs/ops/do-not-touch-register.md` added to prevent concurrent-scope collision. |
| Security/privacy qualification | `PRESERVE BOUNDARY / BLOCK FUTURE PRIVATE WORK` | Public-repo recovery is not private-matter qualification. `SFL-PRIVATE-001` remains later. |
| Rights/license/contribution/distribution posture | `UNKNOWN / BLOCKED FOR REUSE CLAIMS` | Repository is public but no license is selected. Public visibility is not reuse/distribution permission. A future rights pass must resolve source code/docs/media/dependency/public-distribution posture before stronger claims. |
| Release/deploy/transmission | `BLOCKED` | No public deploy, court filing, service, email/provider transmission, or release authority. |
| Framework primitive adoption | `BLOCKED` | 33 Inbox-derived names are promotion intake only. Canonical ownership/version/adopter-lock must come from xi-io.net. |
| External-worker public-safe contract | `PRESERVE + REFRESH` | Provider-neutral worker contract and machine projection exist; refresh discovery/currentness and planning-state separation in this lane. |
| Claude discovery adapter | `PRESERVE + REFRESH` | `CLAUDE.md` remains a thin adapter only. It must point to checkpoint/custody and never become a second policy system. |
| Claude conformance qualification | `BLOCKED UNTIL PR #6 MERGES` | Existing conformance pilot is a test spec, not a passed provider qualification. |

## 4. Planning truth must be split into three planes

This repository must stop asking one document or one status field to carry roadmap, backlog and live branch custody simultaneously.

### A. Strategic / waterfall truth

Answers:

```text
what product we are building
which Saskatchewan source families must ultimately be governed
major legal/source/privacy/security/rights/release obligations
major dependency/gate order
what constitutes product-level readiness
```

Current accepted inputs include the product vision, architecture lock, legal/privacy boundaries and recovery findings. A future accepted PlanRevision should compose them without rewriting history.

### B. Agile / flow truth

Answers:

```text
which bounded work items derive from the accepted plan
which are ready vs blocked
which dependencies must clear first
which one is the next eligible candidate
```

Current recovery dependency order is:

```text
Q1 multi-kit source-family/equivalence semantics
-> Q2 source-family review lanes, one family at a time
-> Q3 portable Project Kernel adoption after framework prerequisite
-> Q4 framework primitive adoption after canonical availability
-> Q5 bounded PR #5 salvage
-> Q6 schema -> triage -> task plan -> UX -> drafting/forms -> private -> optional AI -> egress -> transmission
```

This queue is not blanket branch authority.

### C. Active execution truth

Answers:

```text
what exact ChangeUnit is active now
which branch/PR owns writes
which paths/actions are allowed or blocked
what exact validation/review remains
what the next safe Git action is
```

Current active execution remains:

```text
work_id = SFL-WORK-025
change_unit = SFL-RECOVERY-CLOSEOUT-001
branch = chore/framework-recovery-2026-08-12
PR = #6
next_new_branch = NONE until PR #6 merges and merged main is verified
```

The canonical current-execution detail remains `docs/ops/SFL-GITHUB-EXECUTION-PLAN-2026-08-12.md`; checkpoint/lane files are projections, not competing SSOTs.

Hard separation:

```text
ACCEPTED PLAN != IMPLEMENTED
BACKLOG ITEM != READY
READY != SELECTED
SELECTED != AUTHORIZED
AUTHORIZED != EXECUTED
EXECUTED != VERIFIED
VERIFIED != LEGALLY CURRENT
RENDERED/SENT != FILED OR VALIDLY SERVED
```

## 5. Framework freshness interpretation

### Accepted framework observation

This refresh observed accepted `xi-io.net` main at:

`151213e01e7c715d251273a3f0b7903821f36045`

The previous public-worker export basis was `b27738d8e31937fb996ba8cf2abd4b8bb125d8f9`. The accepted-main change does not by itself authorize product mutation; this re-onboarding record updates the observation and keeps affected adoption state explicit.

### Active framework planning owners

The following framework work materially affects future adoption but must not be treated as already-accepted product contract merely because an issue exists:

```text
#300 Git governance / mutation admission / accepted-main semantics
#306 namespace + registry/read-order reconciliation
#310 Task Context
#313 strategy-to-execution lifecycle
#315 versioned distribution / adopter locks / backfeed
#317 rights / licensing / contribution / distribution qualification
#319 managed-project onboarding / re-onboarding
#321 Work Continuation
#327 planning-promotion graduation smoke test
```

For public external workers these references are provenance/owner pointers only. Required worker obligations must be exported locally; private framework access is not required.

## 6. Claude handoff readiness

### Already present in PR #6

```text
CLAUDE.md
SFL-PUBLIC-MANAGED-WORKER-CONTRACT-v1.md
SFL-PUBLIC-MANAGED-WORKER-CONTRACT-v1.json
SFL-EXTERNAL-WORKER-CONFORMANCE-PILOT-001.md
SFL-GITHUB-EXECUTION-PLAN-2026-08-12.md
```

These are the correct direction: provider-neutral contract first, Claude-specific discovery only as a thin adapter.

### Added/refreshed by this kickoff update

```text
SFL-REONBOARDING-DELTA-2026-08-12.md
ACTIVE_WORK_CHECKPOINT.md
CURRENT_LANE_STATUS.md
do-not-touch-register.md
verification-runbook.md
```

The public worker contract/adapter/validators must point to these current-state surfaces and preserve fail-closed behavior.

### What this still does NOT prove

```text
Claude actually loaded/followed the contract
Claude mutation enforcement hooks exist
provider execution is safe by configuration alone
Task Context/Execution Package is integrated
private legal matter access is authorized
legal source currentness is verified
```

The first Claude qualification remains a **read-only conformance run from a fresh clone after PR #6 is accepted and merged**, with no chat history and no private framework access.

## 7. Post-recovery Claude path

After PR #6 merge + verified `main`:

```text
CLAUDE-CONFORMANCE-001
  fresh clone
  no chat history
  no private xi-io.net access
  read-only / plan-oriented qualification
  hostile scenarios
  complete outcome report

-> WORKER-PREFLIGHT-001
   provider-neutral executable preflight/admission projection

-> CLAUDE-ADAPTER-001
   thin project settings/hooks/rehydration consuming provider-neutral preflight
   no independent policy copy

-> CLAUDE-WORKER-ROLES-001
   bounded custom workers only after common contract works

-> canonical Execution Package adoption when framework prerequisites clear
```

Do not add `.claude/` enforcement machinery to PR #6. That is a later ChangeUnit from verified `main` after the conformance evidence tells us what enforcement is actually required.

## 8. Product work remains source-gated

Claude qualification is development infrastructure. It does not advance legal-source authority.

The product still requires:

```text
SFL-MULTI-KIT-SOURCE-001
-> Kit #3J independent review
-> Kit #2a full forms + process + currentness review
-> Kit #4a full forms + process + currentness review
-> Form 15-52 official comparison/review
-> procedural-reference freshness review
```

For each source family:

```text
exact source identity
-> archive/hash
-> current official comparison
-> complete forms inventory
-> complete line-item catalog
-> complete process/decision/supporting-document/service/filing/follow-up capture
-> discrepancy capture
-> rendered independent review
-> human source approval
-> only then runtime-admissible promotion
```

## 9. Kickoff-package backfeed candidates

These are product evidence to route through existing framework owners, not canonical framework law from this repository.

### Candidate A — kickoff completeness must be domain-aware

A legal self-help kickoff that captures only one reference kit can still pass generic repo/bootstrap checks while missing the product's jurisdiction-wide source-family boundary.

Reusable requirement pressure:

```text
REFERENCE SLICE SELECTED
!= PRODUCT SOURCE COVERAGE PROVEN
```

The kickoff/re-onboarding process should explicitly ask whether the first slice is merely a proving slice or is being mistaken for complete product scope.

### Candidate B — planning planes must be independently discoverable

A fresh worker should not infer strategic plan, agile queue and active execution from the same issue/README/branch recency.

### Candidate C — public external-worker export

A public managed repo that consumes a private framework needs a minimized repo-local worker contract so external agents can obey required constraints without private-framework disclosure.

### Candidate D — source currentness/equivalence is not generic structural validation

Legal/source products need domain-specific promotion blockers for current official comparison, repeated-item equivalence and unresolved procedural conflicts.

### Candidate E — re-onboarding must preserve valid product truth

The August recovery validates the framework direction that existing projects should be classified `PRESERVE/ADD/UPDATE/MIGRATE/NOT_APPLICABLE/BLOCKED/UNKNOWN` rather than overwritten by a new template.

## 10. Current closeout gates

This kickoff refresh is part of PR #6 recovery/control-plane closeout. It does not replace the remaining PR #6 gates.

Before PR #6 can be owner-ready:

```text
R6-A cold-start truth aligned
R6-B blockers explicit
R6-C framework primitive backfeed custody preserved
R6-D PR #5 read-only salvage map complete
R6-E npm run check + git diff --check
R6-F hostile exact-head review
R6-G explicit owner gate
R6-H merge + verify main + receipt + retire
```

Any material byte change after exact-head review invalidates that review.

## 11. Current result

```text
ORIGINAL KICKOFF = HISTORICALLY USEFUL, PRODUCT-SCOPE INCOMPLETE
AUGUST RECOVERY = CORRECT DIRECTION, NOW EXTENDED BY RE-ONBOARDING DELTA
REPOSITORY STANDARDIZATION = IN PROGRESS IN PR #6
STRATEGIC PLAN PROJECTION = MISSING / FUTURE ACCEPTED-MAIN WORK
AGILE QUEUE = PRESENT AS RECOVERY DEPENDENCY ORDER, NOT YET CANONICAL PLAN OBJECT
ACTIVE EXECUTION = PR #6 / SFL-RECOVERY-CLOSEOUT-001
PORTABLE xiio/ ADOPTION = BLOCKED
LEGAL RUNTIME = BLOCKED
CLAUDE DISCOVERY CONTRACT = PRESENT
CLAUDE CONFORMANCE RUN = NOT YET EXECUTED
CLAUDE MUTATION ENFORCEMENT = NOT YET BUILT
NEXT SAFE ACTION = FINISH PR #6 CLOSEOUT, THEN RUN READ-ONLY CLAUDE CONFORMANCE FROM VERIFIED MAIN
```
