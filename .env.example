# No provider, storage, court, or email integration is configured in the bootstrap.
# Copy to .env only after an approved implementation slice defines each variable,
# its data boundary, retention behavior, and disable plan.
APP_ENV=development
